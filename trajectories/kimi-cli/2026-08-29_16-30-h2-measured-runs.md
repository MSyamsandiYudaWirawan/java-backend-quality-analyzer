# Session: h2 measured runs — dual profile, petclinic collapse, blog boot-crash root cause
- Date: 2026-08-29
- Tool: Kimi Code CLI
- Model: kimi-k3
- Human Checkpoint: yes

## Prompt Given

"go start step 3" (the 10 measured docker runs). Mid-run steers:
1. "should we use different container name so it can run paralel?"
2. "alright lets just do sequence we can afford to wait"
3. "hmm 50 doesn tell anything" → re-profile decision
4. "dont delete 50 vus yet mayybe we can combine it with 200 vus" / "what i mean is the report"
5. "should we just lower the score for blog-rest-api of course with good reason"

## Key Decisions Made

- **Sequential runs kept, parallelism rejected.** Parallel would need
  namespaced compose projects/volumes AND would break the identical-envelope
  fairness story — same environment-sensitivity class as the port-5432
  incident that swung baseline ρ 0.493→0.811. Fixed container names treated
  as a feature (enforce one run at a time).
- **Pilot caught two tooling bugs before any measured evidence:** Git Bash
  rewrote `-e K6_JSON_OUT=/hackathon/...` to a Windows path when spawning
  docker.exe → false NOT_TESTABLE (fixed: MSYS_NO_PATHCONV=1 — third instance
  of the documented path-mangling bug class); template textSummary read k6
  metrics at top level instead of `.values` (cosmetic, fixed in both
  templates). All 8 scripts re-rendered pre-measurement.
- **50 VUs → 200 VUs re-profile (human decision).** At 50 VUs every repo
  PASSed thresholds by 5× (vacuous verdicts; no repo near its knee; Hikari-10
  never queues). Human: "50 doesn't tell anything." Re-rendered all scripts
  with vus=200 default and re-ran everything BEFORE any evidence was
  committed, preserving comparability.
- **50-VU reports preserved (human decision):** `evidence/advanced/h2-50vus/`
  snapshot taken mid-sweep minutes before the 200-VU runs overwrote the
  originals (they were uncommitted). Dual profile = efficiency + stress view
  for the Runtime grade.
- **blog-rest-api: score lowered WITH root-cause evidence instead of
  workarounds (human decision).** The docker run died at "target did not
  become healthy" — one native repro (jar + stock MySQL 8.4) found the actual
  cause: HikariPool init fails with "Public Key Retrieval is not allowed";
  the hardcoded tutorial datasource URL assumes the author's local MySQL auth
  setup. Finding cites `boot-diagnosis.log`; no allowPublicKeyRetrieval
  patch, no JWT bootstrap (would only reach the auth wall anyway).
- **Two hollow local clones bypassed:** `targets/spring-petclinic` and
  `targets/springboot-blog-rest-api` are empty git inits; first sweep got
  "no pom.xml at repo root" findings. Re-ran via URL (pipeline clones fresh).

## Agent Output Summary

- 200-VU measured: practice-mvc FAIL (p95 646ms vs caffeine sibling PASS at
  2168 rps — cache is the difference), webflux PASS, webflux-redis PASS
  (tightest tail, max 382ms), module6 PASS, spring-petclinic FAIL (234 rps,
  p95 2191ms, max 8111ms — baseline-100 repo saturating), petclinic-degraded
  FAIL (statistically identical to petclinic, consistent with byte-identical
  runtime code). Checks 100% everywhere — differences are queueing, not errors.
- Findings with cited evidence: blog (boot crash + JWT wall), gs-rest (no
  create endpoint), showcase (javax.xml.bind build failure, fresh build.log).
- Files: all `evidence/advanced/h2/<repo>/{k6-smoke,k6-full}.json`,
  `load-report.{json,md}`, build/boot logs; `evidence/advanced/h2-50vus/`;
  `prompts/README.md` §3 rewritten as handoff contract for a fresh
  blind-scoring session.
- Commit `402f6ac` (pilot fixes), `3f791ee` (step-1 trajectory); measured
  evidence commit proposed at session end.

## Human Checkpoint

- Human reviewed every decision point: parallel-vs-sequential, 200-VU
  re-profile, 50-VU preservation, blog handling (lower score with reason vs
  fight). No manual edits after agent output.
- Human caught that 50-VU results were about to be destroyed mid-sweep —
  preservation snapshot was their call.

## Retries / Corrections

- Pilot v1 false NOT_TESTABLE (path mangling) — fixed, re-rendered, pilot v2
  PASS (practice-mvc 1072 rps).
- First sweep used hollow local clones for petclinic/blog → bogus
  "no pom.xml" findings → re-ran via URL; both measured/found correctly.
- Agent's grep of load-report.md p95 initially matched the "95" in the label
  — corrected by parsing load-report.json directly (parse data, not text).
- k6-ceiling caveat flagged for scoring: mvc-caffeine 2168 rps may approach
  the 1-CPU k6 container's generator ceiling, compressing the top end.
