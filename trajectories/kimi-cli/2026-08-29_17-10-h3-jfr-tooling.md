# Session: h3 tooling — --jfr pipeline, three failure rounds, petclinic lock-contention evidence
- Date: 2026-08-29
- Tool: Kimi Code CLI
- Model: kimi-k3
- Human Checkpoint: yes

## Prompt Given

"okay g start Sanity-check first then do next step" (h3 per prompts/README §3).
Mid-run steers:
1. "i think we create new branch fisrt so not to polute this branch, dont
   forget create the hyphotesis 3 later"
2. "for not testeble service just dont test it use same reason with prev k6
   load test"
3. "check the git ignore"
4. "this is from my last practice it work maybe can be used in reference"
   (old template run-experiment.sh + docker-compose.benchmark.yml JFR recipe)
5. "check the dockerfile this should use eclipse-temurin:21-jre-jammy"
6. "i forgot i also have actuator in myservice practice not sure in other"
7. "okay let stop writing tellme first what is the problem"

## Key Decisions Made

- **Branch hygiene:** `exp/h3-full-pipeline` from `exp/h2-k6-generation` at
  `7500f0e` (h2 KEPT commit) before any edit — h2 branch stays the clean
  reference point (human steer 1).
- **--jfr design:** committed k6 script + slots stay in `h2/<repo>/`
  (immutable inputs); ALL run outputs (k6 JSON, load report, profile.jfr,
  jfr/) land in `h3/<repo>/`. `--jfr` is docker-only (native orchestrate.js
  has no JAVA_OPTS wiring → exit 2 with clear message). JFR recording via
  the pre-existing `JFR_OPTS` compose seam; diagnosis by the pre-existing
  `jfr-diagnose.sh`, unchanged (works on any .jfr path).
- **Hypothesis pre-registered BEFORE any JFR was taken**
  (`evidence/experiments/h3-full-pipeline.md`): 4 falsifiable predictions,
  KEPT/REJECTED criteria vs h2's ρ = 0.954. Human steer 1 anticipated this.
- **NOT_TESTABLE repos get no h3 runs at all (human steer 2):** their h3
  score sheets will cite the h2 findings (boot-diagnosis.log etc.) and keep
  Runtime = 0 by policy §5. Only 7 testable repos run --jfr.
- **gitignore gap found on request (steer 3):** `evidence/*.jfr` was
  top-level only — h3's nested `profile.jfr` + 250 MB event dumps would
  have been committed. Extended rules: ignore `h3/**/*.jfr` and
  `h3/*/jfr/*.txt`, keep reports/JSON — same "regeneratable raw outputs
  out, human-readable reports in" intent as the existing block.
- **In-memory JFR, not disk=true** — the fix, from the human's practice
  template (steer 4). `disk=true` streams chunks to the JFR repository
  during the run; that write mechanism fails on Docker Desktop's Windows
  bind mount ("Could not start recording, not able to write to file" at VM
  init → container exit 1). Old template used in-memory recording +
  `dumponexit=true` → one plain file write at JVM shutdown, which the mount
  tolerates. Verified manually (2.3 MB profile.jfr on the mount). jcmd live
  dumps not an option: temurin 21-jre image ships no jcmd.
- **MSYS_NO_PATHCONV=1 on every compose call (via `env` in the COMPOSE
  array).** h2 patched only the k6 `compose run` calls; h3's exported
  `JFR_OPTS=/jfr-repo/...` got mangled to `C:/Program Files/Git/jfr-repo/...`
  when `up` spawned docker.exe — FOURTH instance of the documented
  path-mangling bug class, first on `up` (h2's JFR_OPTS was empty). Scoped
  via `env` prefix, not a global export: a shell-wide export would disable
  /c/...→C:\ conversion for the node/python calls later in the script.
  Diagnosis method: manual compose run with identical env worked → the
  delta was the pipeline's spawn environment, not the compose config.
- **Dockerfile.target pinned to `eclipse-temurin:21-jre-jammy`** (human
  steer 5) — the floating `21-jre` tag silently moved between h2 and h3
  (21.0.11 → 21.0.12 observed across runs); pin for reproducibility.
- **Actuator worry raised + resolved (steer 6):** healthcheck is already a
  raw TCP-port probe precisely because actuator isn't guaranteed on
  arbitrary repos — no change needed.

## Agent Output Summary

- Files: `run-experiment.sh` (--jfr flag, h3 output isolation, MSYS fix),
  `service/advanced/docker/h2-target.yml` (comment), `Dockerfile.target`
  (jammy pin), `.gitignore` (h3 raw-dump rules),
  `evidence/experiments/h3-full-pipeline.md` (pre-registered hypothesis),
  `tests/unit/test-run-experiment.sh` (+3 cases: --jfr requires --docker;
  --jfr --docker without script exits 2; no h3 dir leak on failed lookup).
- Tests: full unit suite green after each change (9 pipeline bash cases,
  22 spearman, 31 gen-k6, + collect/baseline/analyze-h1/k6-report suites).
- **First measured evidence: spring-petclinic h3 run complete** — k6 FAIL
  (189 rps, p95 2500ms, checks 100%, reproduces the h2 collapse) + JFR
  signal: JavaMonitorEnter 71,738 events p95 407ms CRITICAL, 71,193 on a
  single monitor `org.springframework.boot.loader.net.protocol.jar.
  UrlJarFiles$Cache` (fat-jar classloader global lock). SocketRead 9 events
  (H2 in-memory DB — not DB I/O), GC 34 pauses p99 137ms (minor).
  Pre-registered prediction #1 CONFIRMED: queueing/blocking signal, not GC.
- Commits: `d16809e` (tooling + hypothesis), `8124f93` (fixes + petclinic
  evidence).

## Human Checkpoint

- Human reviewed the problem explanation (steer 7) before approving the
  fix; human supplied the working JFR recipe from their practice template
  (steer 4) which REPLACED the agent's docker-cp workaround (verified
  working but more moving parts — dropped in favor of the proven recipe).
- Human approved each git mutation individually (branch, tooling commit,
  fixes+evidence commit).

## Retries / Corrections

- **Run 1 fail:** "no pom.xml at repo root" — `targets/spring-petclinic` is
  a hollow git init (known from h2, re-discovered). Re-ran by URL. Bogus
  NOT_TESTABLE artifacts deleted (pipeline misfire, not a repo finding).
- **Run 2 fail:** container exit 1 at `up --wait` — `disk=true` JFR chunk
  writes fail on the Windows bind mount. Agent first verified a docker-cp
  workaround (record to /tmp, cp after stop), then the human's template
  showed the simpler in-memory + dumponexit recipe; adopted after manual
  verification.
- **Run 3 fail:** same symptom, different cause — manual compose repro
  worked, pipeline didn't → Git Bash mangled the exported JFR_OPTS path on
  compose `up` (no MSYS_NO_PATHCONV there). Fixed array-wide.
- **Self-correction mid-fix:** agent initially wrote a shell-wide
  `export MSYS_NO_PATHCONV=1`, caught before running that it would break
  node/python /c/... conversion later in the script; rescoped to `env`
  prefix on the compose array only.
