# Session: h3 measured runs, blind JFR scoring, eval — KEPT (ρ = 0.973)
- Date: 2026-08-29
- Tool: Kimi Code CLI
- Model: kimi-k2
- Human Checkpoint: yes

## Prompt Given

"okay commit it then run following your sugestion" (petclinic first), then
"okay later just commit it no need to ask me" (standing commit approval for
this session). No steering during the measured runs or scoring — the rubric
and predictions were pre-registered in `evidence/experiments/
h3-full-pipeline.md` before any JFR was taken.

## Key Decisions Made

- **Runs sequential, 2 batches** (fixed container name/image = one run at a
  time, the h2 envelope-fairness rule). 7 testable repos only; the 3
  NOT_TESTABLE repos got no h3 runs (human decision earlier in the day) —
  their sheets cite the h2 findings, Runtime stays 0 by policy §5.
- **Blind scoring from diagnosis reports only.** Uniform 0–10 jfrProfile
  rubric fixed BEFORE applying to any repo: 10 clean request path; 6 one
  concerning non-dominant mechanism; 4 one critical request-path mechanism
  (monitors p95 > 500ms at 1k+ events, or blocking reads dominating); 2
  multiple compounding critical mechanisms; 0 unmeasurable. Expert ranking
  not consulted until the harness ran. Only the jfrProfile item was
  re-scored — boots/latencyThroughput stay as committed in h2.
- **Severity labels from jfr-diagnose NOT used as scores.** Its thresholds
  mark 2 monitor events "CRITICAL" (p95-based); scoring weighed count ×
  duration on the request path instead (webflux's 2 events = clean;
  petclinic's 71,738 = collapse). Envelope-normal GC (p99 87–137ms on the
  2-CPU G1 envelope, all repos) carries no deduction.
- **The grading rule applied against k6 verdict when evidence demanded:**
  module6 PASSed k6 (1219 rps) but showed 5,018 monitor events p95 1310ms
  (Tomcat RecycledProcessors + shared HashMap) → 4/10, held at a tie with
  webflux-redis (clean profile, 10/10) instead of rising above it. "Strong
  throughput with a critical hotspot < modest throughput with a clean
  profile."
- **cp1252 corruption caught by the harness, fixed before consumption:**
  Python `json.dump` on Windows defaults to the locale encoding — the first
  scoring pass wrote em-dashes as cp1252 0x97, evaluate.py's JSON parse
  failed on practice-mvc. Sheets restored via git checkout, re-scored with
  `encoding="utf-8"`, all 10 verified valid. No corrupted score reached the
  eval.
- **Score-sheet edit in place** (h2 did the same): h2 reproducibility is
  preserved by git history + the `evidence/eval/h2/` snapshot.

## Agent Output Summary

- Measured: 7/7 testable repos end-to-end (`--docker --jfr`): k6 + JFR +
  diagnosis report each. Evidence at `evidence/advanced/h3/<repo>/`
  (commits `8124f93`, `d8656ac`, `094437b`).
- Prediction scorecard: 4/4 confirmed — (1) petclinic/degraded collapse =
  fat-jar classloader lock `UrlJarFiles$Cache` (71k monitor events p95
  ~400ms), not GC; (2) caffeine = ~2.7x fewer DB reads/request, not faster
  reads; (3) webflux/redis clean of blocking; (4) JFR item off 0-for-all.
- Scored: jfrProfile webflux 10, redis 10, caffeine 6, mvc 4, module6 4,
  petclinic 2, degraded 2; NOT_TESTABLE 3 stay 0.
- **Eval h3: ρ = 0.973** (h2 0.954, baseline 0.811), bounds [0.964, 0.976],
  pairs 42/2/1 (h2 41/3/1). mvc/degraded tie broke in the expert's
  direction. Verdict **KEPT** per pre-registered criteria.
- Files: 10 score sheets (in place), `evidence/eval/h3/`,
  `evidence/experiments/h3-full-pipeline.md` (Results filled), README §3
  state. Full unit suite green before the scoring session.

## Human Checkpoint

- Human approved the tooling+evidence commits individually, then granted
  standing commit approval for the session ("just commit it no need to ask
  me"). Scoring itself ran with no human input — blind as designed.

## Retries / Corrections

- Retry: first eval run crashed on cp1252-corrupted sheet (0x97 em-dash at
  position 3128) → git restore + re-score with explicit utf-8 + validity
  check across all 10 sheets → eval green.
- No scoring corrections: the rubric was applied as pre-registered; no
  score was adjusted after the harness ran.
