# README files at repo root (size in bytes)

# License files at repo root

# Config files (application*.properties/yml/yaml, excluding target/)

# Committed binaries (jar/class/war/ear, excluding target/)
.mvn/wrapper/maven-wrapper.jar
gradle/wrapper/gradle-wrapper.jar

# Suspicious secret-looking lines in tracked config/source (password|secret|apikey|token = <value>)
